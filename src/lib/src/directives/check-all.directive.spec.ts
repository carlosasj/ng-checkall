import { CheckAllDirective } from './check-all.directive';

describe('CheckAllDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckAllDirective();
    expect(directive).toBeTruthy();
  });
});

import { CheckContainerDirective } from './check-container.directive';

describe('CheckContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckContainerDirective();
    expect(directive).toBeTruthy();
  });
});

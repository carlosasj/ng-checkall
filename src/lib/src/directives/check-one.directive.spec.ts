import { CheckOneDirective } from './check-one.directive';

describe('CheckOneDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckOneDirective();
    expect(directive).toBeTruthy();
  });
});
